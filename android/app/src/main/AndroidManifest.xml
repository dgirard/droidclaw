<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.RECORD_AUDIO"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_REMOTE_MESSAGING"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION"/>
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <uses-permission android:name="android.permission.WAKE_LOCK"/>
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>
    <uses-permission android:name="com.android.alarm.permission.SET_ALARM"/>
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>
    <uses-permission android:name="android.permission.VIBRATE"/>
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM"/>
    <uses-permission android:name="android.permission.READ_CONTACTS"/>
    <uses-permission android:name="android.permission.READ_CALENDAR"/>
    <uses-permission android:name="android.permission.WRITE_CALENDAR"/>
    <uses-permission android:name="android.permission.CAMERA"/>
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK"/>
    <!-- U7 wake word: a SECOND, dedicated foreground service of type
         microphone hosts the always-listening KWS capture. It is started
         ONLY from the app foreground (Android 14+ forbids background start of
         a microphone-type FGS) and is therefore dead after reboot until the
         app is opened once — documented & surfaced in Settings → Voice. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MICROPHONE"/>
    <application
        android:label="ARaccoon"
        android:name="${applicationName}"
        android:allowBackup="false"
        android:fullBackupContent="false"
        android:networkSecurityConfig="@xml/network_security_config"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
        <!-- Foreground-service types (reviewed U6 — both are genuinely used
             by the service isolate, see lib/core/agent/service_agent_factory.dart):
             - remoteMessaging: Telegram long-polling in BackgroundTaskHandler.
             - location: get_location/get_address tools registered in the
               service-isolate tool registry for autonomous cron tasks. -->
        <service
            android:name="com.pravera.flutter_foreground_task.service.ForegroundService"
            android:foregroundServiceType="remoteMessaging|location"
            android:exported="false" />
        <!-- exported="true" is required for a media3 MediaSessionService: the
             system binds to it for the media notification and media-button /
             Bluetooth routing. Caller verification happens in
             RadioPlaybackService.onGetSession (unknown callers are rejected). -->
        <service
            android:name=".RadioPlaybackService"
            android:foregroundServiceType="mediaPlayback"
            android:exported="true">
            <intent-filter>
                <action android:name="androidx.media3.session.MediaSessionService" />
            </intent-filter>
        </service>
        <!-- U7 wake word: dedicated microphone foreground service.

             WHY A SEPARATE NATIVE SERVICE (not the flutter_foreground_task
             service): flutter_foreground_task owns com.pravera...ForegroundService
             and that service boot-starts (autoRunOnBoot for cron/Telegram).
             A microphone-type FGS CANNOT be background-started on Android 14+,
             so adding "microphone" to the existing service's serviceTypes
             would make its boot-start crash. The plan's decision (KTD) is a
             SEPARATE service started from the foreground only — implemented as
             this dedicated Kotlin foreground service, mirroring the
             RadioPlaybackService pattern. It is NOT exported (unlike the
             media service, the system never binds to it) and carries no
             notification "stop" action (documented UI learning: the only off
             switch is the Settings → Voice toggle).

             SPIKE-GATED: the native service body and the Dart<->native PCM
             bridge ship once spike S2 confirms on-device viability (mic FGS
             survives backgrounding + battery within
             wakeWordMaxBatteryPctPerDay). Until then this declaration reserves
             the manifest entry and the Dart arbitration layer
             (lib/core/services/wake_word_service.dart) runs against seams. The
             service class is commented out so the build does not require a
             not-yet-written Kotlin class.
        -->
        <!--
        <service
            android:name=".WakeWordService"
            android:foregroundServiceType="microphone"
            android:exported="false" />
        -->
        <!-- flutter_local_notifications receivers -->
        <receiver
            android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationBootReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED"/>
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED"/>
            </intent-filter>
        </receiver>
        <receiver
            android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationReceiver"
            android:exported="false" />
        <receiver
            android:name="com.dexterous.flutterlocalnotifications.ActionBroadcastReceiver"
            android:exported="false" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
        <intent>
            <action android:name="android.intent.action.SET_ALARM"/>
        </intent>
        <intent>
            <action android:name="android.intent.action.SET_TIMER"/>
        </intent>
    </queries>
</manifest>
